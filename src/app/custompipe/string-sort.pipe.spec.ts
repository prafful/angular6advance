import { StringSortPipe } from './string-sort.pipe';

describe('StringSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StringSortPipe();
    expect(pipe).toBeTruthy();
  });
});
