import { AutoSearchPipe } from './auto-search.pipe';

describe('AutoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
